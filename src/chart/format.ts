// Default compartilhado entre o preview no canvas (components/FieldBox/ChartField.tsx)
// e o desenho real no PDF (pdf/render/renderChart.ts) — mesmo valor nos dois lugares
// pra preview bater com o PDF gerado quando o schema não define um tamanho.
export const DEFAULT_CHART_LEGEND_FONT_SIZE = 8;
