// A default shared between the canvas preview (components/FieldBox/ChartField.tsx)
// and the real drawing in the PDF (pdf/render/renderChart.ts) — the same value in
// both places so the preview matches the generated PDF when the schema sets no size.
export const DEFAULT_CHART_LEGEND_FONT_SIZE = 8;
