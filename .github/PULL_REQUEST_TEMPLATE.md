## What this changes

<!-- One paragraph. What behavior is different after this PR. -->

## Why

<!-- The problem. Link the issue if there is one: Fixes #123 -->

## Checklist

- [ ] `npm run typecheck && npm run lint && npm test && npm run build` passes
- [ ] A bug fix comes with the test that would have caught it
- [ ] User-facing change updated `README.md` **and** `README_pt-BR.md`
- [ ] New UI strings added to both `en.ts` and `pt-BR.ts`
- [ ] `CHANGELOG.md` and `CHANGELOG.pt-BR.md` updated
- [ ] Numbers in docs re-measured, not adjusted by hand

## Breaking change?

<!-- If test/publicSurface.test.ts changed, it is. Say what breaks and
     what the migration is. If not, write "No". -->
